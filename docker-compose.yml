services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
    ports:
      - "8080:8080"
    env_file:
      - .env
    command: mvn spring-boot:run